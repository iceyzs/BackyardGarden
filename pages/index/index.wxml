<!--index.wxml-->
<view class="container">
  <view class="userinfo">
      <view class="indexPage">
        <view  class="indexPage1">
          <view class="indexPage2">後</view>
          <view class="indexPage2">花</view>
          <view class="indexPage2">園</view>
          <view class="indexPage2">之</view>
          <view class="indexPage2">冢</view>
        </view>
      </view>
      <button class="getUserInfoBtn" wx:if="{{!hasUserInfo && canIUse}}" open-type="getUserInfo" bindgetuserinfo="getUserInfo">
        开门
      </button>
    <block wx:else>
      <!-- 
        <image bindtap="bindViewTap" class="userinfo-avatar" src="{{userInfo.avatarUrl}}" mode="cover"></image>
        <text class="userinfo-nickname">{{userInfo.nickName}}</text> 
      -->
    </block>
  </view>
  <view class="usermotto">
    <text class="user-motto">{{motto}}</text>
  </view>
</view>
